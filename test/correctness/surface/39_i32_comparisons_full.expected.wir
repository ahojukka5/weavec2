(core-module (core-version 1) (decls (fn main (params) (returns i32) (do (if (condition (eq_i32 (const_i32 42) (const_i32 42))) (then (do)) (else (do (return (const_i32 0))))) (if (condition (ne_i32 (const_i32 42) (const_i32 0))) (then (do)) (else (do (return (const_i32 0))))) (if (condition (lt_i32 (const_i32 1) (const_i32 2))) (then (do)) (else (do (return (const_i32 0))))) (if (condition (le_i32 (const_i32 2) (const_i32 2))) (then (do)) (else (do (return (const_i32 0))))) (if (condition (gt_i32 (const_i32 3) (const_i32 2))) (then (do)) (else (do (return (const_i32 0))))) (if (condition (ge_i32 (const_i32 4) (const_i32 4))) (then (do (return (const_i32 42)))) (else (do (return (const_i32 0)))))))))
