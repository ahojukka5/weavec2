; Performance: 0026_bool_return
; Why hard: Smoke baseline for bool as i1. Failures here usually indicate broken lowering shared by every later fixture.
; Reveals: returns boolean from helper
; If LLVM regresses: Returns i32 0/1 with wrong width at callsite.
(core-module
  (core-version 1)
  (decls
    (fn same
      (params
        (x bool)
      ) ;; params
      (returns bool)
      (do
        (return
          (param_get x)
        ) ;; return
      ) ;; do
    ) ;; fn
    (fn main
      (params)
      (returns i32)
      (do
        (if
          (condition
            (call_bool same
              (const_bool true)
            ) ;; call_bool
          ) ;; condition
          (then
            (do
              (return
                (const_i32 42)
              ) ;; return
            ) ;; do
          ) ;; then
          (else
            (do
              (return
                (const_i32 0)
              ) ;; return
            ) ;; do
          ) ;; else
        ) ;; if
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
