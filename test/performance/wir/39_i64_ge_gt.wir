(core-module
  (core-version 1)
  (decls
    (fn main
      (params)
      (returns i32)
      (do

        (if
          (condition
            (gt_i64
              (const_i64 10)
              (const_i64 9)
            ) ;; gt_i64
          ) ;; condition
          (then
            (do)
          ) ;; then
          (else
            (do
              (return
                (const_i32 0)
              ) ;; return
            ) ;; do
          ) ;; else
        ) ;; if

        (if
          (condition
            (ge_i64
              (const_i64 10)
              (const_i64 10)
            ) ;; ge_i64
          ) ;; condition
          (then
            (do
              (return
                (const_i32 42)
              ) ;; return
            ) ;; do
          ) ;; then
          (else
            (do
              (return
                (const_i32 0)
              ) ;; return
            ) ;; do
          ) ;; else
        ) ;; if

      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
