; Performance: 0076_trial_division_prime_i32
; Why hard: Primality by trial division up to sqrt.
; Reveals: Loop with mod, early exit flag.
; If LLVM regresses: sqrt bound too large/small; flag never set on composite.
(core-module
  (core-version 1)
  (decls
    (fn is_prime
      (params
        (n i32)
      ) ;; params
      (returns i32)
      (do
        (if
          (condition
            (lt_i32
              (param_get n)
              (const_i32 2)
            ) ;; lt_i32
          ) ;; condition
          (then
            (do
              (return
                (const_i32 0)
              ) ;; return
            ) ;; do
          ) ;; then
          (else
            (do)
          ) ;; else
        ) ;; if

        (let d i32
          (const_i32 2)
        ) ;; let

        (let prime i32
          (const_i32 1)
        ) ;; let

        (while
          (condition
            (and_bool
              (ne_i32
                (local_get prime)
                (const_i32 0)
              ) ;; ne_i32
              (le_i32
                (mul_i32
                  (local_get d)
                  (local_get d)
                ) ;; mul_i32
                (param_get n)
              ) ;; le_i32
            ) ;; and_bool
          ) ;; condition
          (do
            (if
              (condition
                (eq_i32
                  (mod_i32
                    (param_get n)
                    (local_get d)
                  ) ;; mod_i32
                  (const_i32 0)
                ) ;; eq_i32
              ) ;; condition
              (then
                (do
                  (set prime
                    (const_i32 0)
                  ) ;; set
                ) ;; do
              ) ;; then
              (else
                (do)
              ) ;; else
            ) ;; if

            (set d
              (add_i32
                (local_get d)
                (const_i32 1)
              ) ;; add_i32
            ) ;; set
          ) ;; do
        ) ;; while

        (return
          (local_get prime)
        ) ;; return
      ) ;; do
    ) ;; fn

    (fn main
      (params)
      (returns i32)
      (do
        (return
          (add_i32
            (call_i32 is_prime
              (const_i32 97)
            ) ;; call_i32
            (mul_i32
              (call_i32 is_prime
                (const_i32 91)
              ) ;; call_i32
              (const_i32 10)
            ) ;; mul_i32
          ) ;; add_i32
        ) ;; return
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
