(core-module
  (core-version 1)
  (decls
    (fn is_answer
      (params
        (x i32)
      ) ;; params
      (returns bool)
      (do
        (return
          (eq_i32
            (param_get x)
            (const_i32 42)
          ) ;; eq_i32
        ) ;; return
      ) ;; do
    ) ;; fn

    (fn main
      (params)
      (returns i32)
      (do
        (if
          (condition
            (call_bool is_answer
              (const_i32 42)
            ) ;; call_bool
          ) ;; condition
          (then
            (do
              (return
                (const_i32 42)
              ) ;; return
            ) ;; do
          ) ;; then
          (else
            (do
              (return
                (const_i32 0)
              ) ;; return
            ) ;; do
          ) ;; else
        ) ;; if
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
