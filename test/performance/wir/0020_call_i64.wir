(core-module
  (core-version 1)
  (decls
    (fn answer64
      (params)
      (returns i64)
      (do
        (return
          (const_i64 42)
        ) ;; return
      ) ;; do
    ) ;; fn
    (fn main
      (params)
      (returns i32)
      (do
        (return
          (cast_i64_to_i32
            (call_i64 answer64)
          ) ;; cast_i64_to_i32
        ) ;; return
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
