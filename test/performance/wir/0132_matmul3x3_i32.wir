; Stress: 3x3 matrix multiply row-major on heap
; Reveals: nested loops, repeated loads/stores, induction on 2D index
(core-module
  (core-version 1)
  (decls
    (extern malloc
      (params
        (size i64)
      ) ;; params
      (returns ptr)
    ) ;; extern

    (extern free
      (params
        (p ptr)
      ) ;; params
      (returns void)
    ) ;; extern

    (fn at
      (params
        (m ptr)
        (r i32)
        (c i32)
      ) ;; params
      (returns ptr)
      (do
        (return
          (array_i32_at
            (param_get m)
            (cast_i32_to_i64
              (add_i32
                (mul_i32
                  (param_get r)
                  (const_i32 3)
                ) ;; mul_i32
                (param_get c)
              ) ;; add_i32
            ) ;; cast_i32_to_i64
          ) ;; array_i32_at
        ) ;; return
      ) ;; do
    ) ;; fn

    (fn matmul3
      (params
        (a ptr)
        (b ptr)
        (out ptr)
      ) ;; params
      (returns void)
      (do
        (let i i32
          (const_i32 0)
        ) ;; let

        (while
          (condition
            (lt_i32
              (local_get i)
              (const_i32 3)
            ) ;; lt_i32
          ) ;; condition
          (do
            (let j i32
              (const_i32 0)
            ) ;; let

            (while
              (condition
                (lt_i32
                  (local_get j)
                  (const_i32 3)
                ) ;; lt_i32
              ) ;; condition
              (do
                (let sum i32
                  (const_i32 0)
                ) ;; let

                (let k i32
                  (const_i32 0)
                ) ;; let

                (while
                  (condition
                    (lt_i32
                      (local_get k)
                      (const_i32 3)
                    ) ;; lt_i32
                  ) ;; condition
                  (do
                    (set sum
                      (add_i32
                        (local_get sum)
                        (mul_i32
                          (load_i32
                            (call_ptr at
                              (param_get a)
                              (local_get i)
                              (local_get k)
                            ) ;; call_ptr
                          ) ;; load_i32
                          (load_i32
                            (call_ptr at
                              (param_get b)
                              (local_get k)
                              (local_get j)
                            ) ;; call_ptr
                          ) ;; load_i32
                        ) ;; mul_i32
                      ) ;; add_i32
                    ) ;; set

                    (set k
                      (add_i32
                        (local_get k)
                        (const_i32 1)
                      ) ;; add_i32
                    ) ;; set
                  ) ;; do
                ) ;; while

                (store_i32
                  (call_ptr at
                    (param_get out)
                    (local_get i)
                    (local_get j)
                  ) ;; call_ptr
                  (local_get sum)
                ) ;; store_i32

                (set j
                  (add_i32
                    (local_get j)
                    (const_i32 1)
                  ) ;; add_i32
                ) ;; set
              ) ;; do
            ) ;; while

            (set i
              (add_i32
                (local_get i)
                (const_i32 1)
              ) ;; add_i32
            ) ;; set
          ) ;; do
        ) ;; while

        (return_void)
      ) ;; do
    ) ;; fn

    (fn main
      (params)
      (returns i32)
      (do
        (let a ptr
          (call_ptr malloc
            (const_i64 36)
          ) ;; call_ptr
        ) ;; let

        (let b ptr
          (call_ptr malloc
            (const_i64 36)
          ) ;; call_ptr
        ) ;; let

        (let out ptr
          (call_ptr malloc
            (const_i64 36)
          ) ;; call_ptr
        ) ;; let

        (if
          (condition
            (or_bool
              (eq_ptr
                (local_get a)
                (const_null)
              ) ;; eq_ptr
              (or_bool
                (eq_ptr
                  (local_get b)
                  (const_null)
                ) ;; eq_ptr
                (eq_ptr
                  (local_get out)
                  (const_null)
                ) ;; eq_ptr
              ) ;; or_bool
            ) ;; or_bool
          ) ;; condition
          (then
            (do
              (return
                (const_i32 0)
              ) ;; return
            ) ;; do
          ) ;; then
          (else
            (do)
          ) ;; else
        ) ;; if

        (store_i32 (call_ptr at (local_get a) (const_i32 0) (const_i32 0)) (const_i32 1))
        (store_i32 (call_ptr at (local_get a) (const_i32 0) (const_i32 1)) (const_i32 2))
        (store_i32 (call_ptr at (local_get a) (const_i32 0) (const_i32 2)) (const_i32 0))
        (store_i32 (call_ptr at (local_get a) (const_i32 1) (const_i32 0)) (const_i32 0))
        (store_i32 (call_ptr at (local_get a) (const_i32 1) (const_i32 1)) (const_i32 1))
        (store_i32 (call_ptr at (local_get a) (const_i32 1) (const_i32 2)) (const_i32 0))
        (store_i32 (call_ptr at (local_get a) (const_i32 2) (const_i32 0)) (const_i32 1))
        (store_i32 (call_ptr at (local_get a) (const_i32 2) (const_i32 1)) (const_i32 1))
        (store_i32 (call_ptr at (local_get a) (const_i32 2) (const_i32 2)) (const_i32 1))

        (store_i32 (call_ptr at (local_get b) (const_i32 0) (const_i32 0)) (const_i32 1))
        (store_i32 (call_ptr at (local_get b) (const_i32 0) (const_i32 1)) (const_i32 0))
        (store_i32 (call_ptr at (local_get b) (const_i32 0) (const_i32 2)) (const_i32 0))
        (store_i32 (call_ptr at (local_get b) (const_i32 1) (const_i32 0)) (const_i32 0))
        (store_i32 (call_ptr at (local_get b) (const_i32 1) (const_i32 1)) (const_i32 1))
        (store_i32 (call_ptr at (local_get b) (const_i32 1) (const_i32 2)) (const_i32 0))
        (store_i32 (call_ptr at (local_get b) (const_i32 2) (const_i32 0)) (const_i32 0))
        (store_i32 (call_ptr at (local_get b) (const_i32 2) (const_i32 1)) (const_i32 0))
        (store_i32 (call_ptr at (local_get b) (const_i32 2) (const_i32 2)) (const_i32 1))

        (call_void matmul3
          (local_get a)
          (local_get b)
          (local_get out)
        ) ;; call_void

        (let ans i32
          (load_i32
            (call_ptr at
              (local_get out)
              (const_i32 2)
              (const_i32 2)
            ) ;; call_ptr
          ) ;; load_i32
        ) ;; let

        (call_void free
          (local_get a)
        ) ;; call_void
        (call_void free
          (local_get b)
        ) ;; call_void
        (call_void free
          (local_get out)
        ) ;; call_void

        (return
          (local_get ans)
        ) ;; return
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
