; Performance: 0031_not_bool
; Why hard: Smoke baseline for boolean not. Failures here usually indicate broken lowering shared by every later fixture.
; Reveals: not_bool on predicate
; If LLVM regresses: Emits xor i32 1 instead of xor i1.
(core-module
  (core-version 1)
  (decls
    (fn main
      (params)
      (returns i32)
      (do
        (if
          (condition
            (not_bool
              (const_bool false)
            ) ;; not_bool
          ) ;; condition
          (then
            (do
              (return
                (const_i32 42)
              ) ;; return
            ) ;; do
          ) ;; then
          (else
            (do
              (return
                (const_i32 0)
              ) ;; return
            ) ;; do
          ) ;; else
        ) ;; if
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
