; generated by weavec2
; source: test/performance/wir/08_while.wir
; core-version: 1

; function: main
; params: none
; returns: i32
define i32 @main() {
entry:
  %i.addr = alloca i32
  %sum.addr = alloca i32
  ; let i
  store i32 0, ptr %i.addr
  ; let sum
  store i32 0, ptr %sum.addr
  ; while condition
  br label %while.pre
while.pre:
  %sum.init0 = load i32, ptr %sum.addr
  %i.init0 = load i32, ptr %i.addr
  br label %while.cond
while.cond:
  %sum.phi0 = phi i32 [%sum.init0, %while.pre], [%sum.next0, %while.body]
  %i.phi0 = phi i32 [%i.init0, %while.pre], [%i.next0, %while.body]
  %t0 = icmp slt i32 %i.phi0, 7
  br i1 %t0, label %while.body, label %while.end
while.body:
  ; while body
  ; set sum
  %t1 = add i32 %sum.phi0, 6
  %sum.next0 = add i32 %t1, 0
  ; set i
  %t2 = add i32 %i.phi0, 1
  %i.next0 = add i32 %t2, 0
  br label %while.cond
while.end:
  ; return
  ret i32 %sum.phi0
}

