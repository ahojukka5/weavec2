; generated by weavec2
; source: test/performance/wir/0158_fibonacci30_i64.wir
; core-version: 1

; function: main
; params: none
; returns: i32
define i32 @main() {
entry:
  %i.addr = alloca i32
  %prev.addr = alloca i64
  %cur.addr = alloca i64
  ; let i
  store i32 2, ptr %i.addr
  ; let prev
  store i64 0, ptr %prev.addr
  ; let cur
  store i64 1, ptr %cur.addr
  ; while condition
  br label %while.pre
while.pre:
  %prev.init0 = load i64, ptr %prev.addr
  %cur.init0 = load i64, ptr %cur.addr
  %i.init0 = load i32, ptr %i.addr
  br label %while.cond
while.cond:
  %prev.phi0 = phi i64 [%prev.init0, %while.pre], [%cur.phi0, %while.latch]
  %cur.phi0 = phi i64 [%cur.init0, %while.pre], [%cur.next0, %while.latch]
  %i.phi0 = phi i32 [%i.init0, %while.pre], [%i.next0, %while.latch]
  %t0 = icmp sle i32 %i.phi0, 30
  br i1 %t0, label %while.body, label %while.exit-merge
while.body:
  ; while body
  ; let next (deferred)
  ; set prev
  ; set cur
  %cur.next0 = add i64 %prev.phi0, %cur.phi0
  ; set i
  %i.next0 = add i32 %i.phi0, 1
  br label %while.latch
while.latch:
  br label %while.cond
while.exit-merge:
  ; sync loop-carried locals to stack
  store i64 %prev.phi0, ptr %prev.addr
  store i64 %cur.phi0, ptr %cur.addr
  store i32 %i.phi0, ptr %i.addr
  br label %while.end
while.end:
  ; return
  %t1 = load i64, ptr %cur.addr
  %t2 = srem i64 %t1, 1000000007
  %t3 = trunc i64 %t2 to i32
  ret i32 %t3
}

