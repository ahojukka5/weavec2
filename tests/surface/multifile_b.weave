; multifile_b.weave — uses the constant from multifile_a
(program
  (name "multifile_b")
  (version "0.1")
  (entry main
    (params)
    (returns i32)
    (do
      (return (cast_i64_to_i32 (call_i64 MAGIC)))))
)
