(core-module (core-version 1) (decls (fn main (params) (returns i32) (do (if (condition (and_bool (const_bool true) (or_bool (const_bool false) (const_bool true)))) (then (do (return (const_i32 42)))) (else (do (return (const_i32 0)))))))))
