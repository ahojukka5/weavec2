(core-module
  (core-version 1)
  (decls
    (fn main
      (params)
      (returns i32)
      (do
        (if
          (condition
            (lt_i64
              (const_i64 1)
              (const_i64 2)
            ) ;; lt_i64
          ) ;; condition
          (then
            (do
              (if
                (condition
                  (le_i64
                    (const_i64 2)
                    (const_i64 2)
                  ) ;; le_i64
                ) ;; condition
                (then
                  (do
                    (if
                      (condition
                        (ne_i64
                          (const_i64 3)
                          (const_i64 4)
                        ) ;; ne_i64
                      ) ;; condition
                      (then
                        (do
                          (return
                            (const_i32 42)
                          ) ;; return
                        ) ;; do
                      ) ;; then
                      (else
                        (do
                          (return
                            (const_i32 0)
                          ) ;; return
                        ) ;; do
                      ) ;; else
                    ) ;; if
                  ) ;; do
                ) ;; then
                (else
                  (do
                    (return
                      (const_i32 0)
                    ) ;; return
                  ) ;; do
                ) ;; else
              ) ;; if
            ) ;; do
          ) ;; then
          (else
            (do
              (return
                (const_i32 0)
              ) ;; return
            ) ;; do
          ) ;; else
        ) ;; if
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
